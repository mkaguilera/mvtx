#ifndef __PARSER_H__
#define __PARSER_H__

#include <cstdint>
#include <cstring>
#include <vector>

// Definition of types.
enum Type { TBEGIN, TEND, TRUN, TGET, TPUT, TEQUAL, TFLOAT, TINTEGER, TVAR };


/*
typedef struct {
	Type type;
	std::string *s;
	int n;
	Node **children;
} Node;
*/

#endif
